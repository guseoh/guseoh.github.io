---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
import { buildCategorySummary } from "../../utils/categories";

const posts = await getCollection("blog", ({ data }) => !data.draft);
const categorySummary = buildCategorySummary(posts);
---

<BaseLayout title="guseo.dev | Categories" description="Category archive index">
  <section class="hero">
    <h1>Categories</h1>
    <p>Browse notes by domain and implementation context.</p>
  </section>

  <ul class="post-list" style="margin-top: 1rem;">
    {categorySummary.map(({ slug, name, count }) => (
      <li class="card category-card-row">
        <a class="badge badge--topic" href={`/categories/${slug}/`}>
          <span>{name}</span>
          <strong>{count}</strong>
        </a>
      </li>
    ))}
  </ul>
</BaseLayout>
